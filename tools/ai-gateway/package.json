{
    "name": "openai-quota-proxy",
    "version": "1.0.0",
    "description": "Docker commands for openai-quota-proxy",
    "scripts": {
      "build": "docker build -t 7frank/openai-quota-proxy .",
      "start": "docker run -d -p 5000:5000 --name openai-quota-proxy --env-file .env 7frank/openai-quota-proxy",
      "stop": "docker stop openai-quota-proxy && docker rm openai-quota-proxy",
      "attach": "docker attach openai-quota-proxy",
      "bash": "docker run -it 7frank/openai-quota-proxy /bin/bash"
    },
    "author": "",
    "license": "ISC"
  }
  