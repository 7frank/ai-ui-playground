/**
 * This function takes in a number and returns its square value.
 *
 * @param {number} num - The number to be squared.
 * @returns {number} The square value of the input number.
 */
function squareNumber(num: number): number {
  return num * num;
}
